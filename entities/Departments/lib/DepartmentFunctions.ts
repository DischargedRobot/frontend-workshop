import { IDepartment } from "./DepartmentType"

// const departmentFucntion = {

//     getDepartmentByDepthLevel: (departments: IDepartment[], department: IDepartment, depthLevel: number): IDepartment => {
//         return departments.find((dep: IDepartment) => {
//             return department.id === dep.id
//         })?.children
//     }
// }
